/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['platform.theverge.com', 'jamesgmartin.center', 'i.snap.as', 'img.freepik.com'],
  },
};

export default nextConfig;
